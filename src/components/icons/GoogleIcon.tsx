import { SVGAttributes } from 'react';
import Google from 'assets/images/icon_google.svg?react';

const GoogleIcon: React.FC<SVGAttributes<SVGElement>> = (props) => (
  <Google {...props} />
);

export default GoogleIcon;
